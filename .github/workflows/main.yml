name: First Workflow

on:
  push: 
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: print a welcome message
        run: echo "Welcome To Your First Workflow using GitHub Actions"

      - name: Checkout repo
        uses: actions/checkout@v7.0.0

      - name: List repo files
        run: ls -la   

      
        
